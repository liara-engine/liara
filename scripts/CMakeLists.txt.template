# GENERATED FILE - DO NOT EDIT
cmake_minimum_required(VERSION 3.29)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
    message(FATAL_ERROR
            "In-source builds are not supported. Build into build/<preset-name>/ instead, which is "
            "what the presets already do:\n"
            "    cmake --preset linux-debug-clang\n"
            "Remove the CMakeCache.txt and CMakeFiles/ this attempt just created before retrying.")
endif()

project(LiaraWorkspace LANGUAGES C CXX)
set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS OFF)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
enable_testing()

# Output directories
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib")

# RPATH settings
if(NOT MSVC)
    set(CMAKE_BUILD_RPATH_USE_ORIGIN ON)
    set(CMAKE_BUILD_RPATH "$ORIGIN")
    set(CMAKE_INSTALL_RPATH "$ORIGIN")
endif()

# Linker selection
if(NOT MSVC)
    find_program(MOLD_EXECUTABLE mold)
    if(MOLD_EXECUTABLE)
        set(CMAKE_LINKER_TYPE MOLD)
        message(STATUS "Using mold as the linker")
    else()
        find_program(LLD_EXECUTABLE ld.lld lld)
        if(LLD_EXECUTABLE)
            set(CMAKE_LINKER_TYPE LLD)
            message(STATUS "Using lld as the linker")
        endif()
    endif()
endif()

# Compiler cache selection
find_program(SCCACHE_EXECUTABLE sccache)
find_program(CCACHE_EXECUTABLE ccache)
if(SCCACHE_EXECUTABLE)
    set(CMAKE_C_COMPILER_LAUNCHER "${SCCACHE_EXECUTABLE}")
    set(CMAKE_CXX_COMPILER_LAUNCHER "${SCCACHE_EXECUTABLE}")
    message(STATUS "Using sccache as the compiler cache")
elseif(CCACHE_EXECUTABLE)
    set(CMAKE_C_COMPILER_LAUNCHER "${CCACHE_EXECUTABLE}")
    set(CMAKE_CXX_COMPILER_LAUNCHER "${CCACHE_EXECUTABLE}")
    message(STATUS "Using ccache as the compiler cache")
endif()

if(LIARA_LAUNCHER_MODULE_LOADING STREQUAL "runtime")
    message(WARNING "Experimental: LIARA_LAUNCHER_MODULE_LOADING=runtime. "
                    "The launcher loads modules at runtime.")
endif()

if(LIARA_IPO)
    include(CheckIPOSupported)
    check_ipo_supported(RESULT ipo_ok OUTPUT ipo_why)
    if(NOT ipo_ok)
        message(FATAL_ERROR "LIARA_IPO=ON but this toolchain cannot do it:\n${ipo_why}")
    endif()
endif()

# MODULES_PLACEHOLDER

if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/../launcher/CMakeLists.txt")
    add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/../launcher" launcher)
endif()
